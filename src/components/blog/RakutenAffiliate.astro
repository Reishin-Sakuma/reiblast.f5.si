---
interface Props {
  html: string;
  htmlMobile?: string;
}
const { html, htmlMobile } = Astro.props;
---
<div class={`rakuten-affiliate${htmlMobile ? ' rakuten-desktop' : ''}`}>
  <Fragment set:html={html} />
</div>
{htmlMobile && (
  <div class="rakuten-affiliate rakuten-mobile">
    <Fragment set:html={htmlMobile} />
  </div>
)}

<style>
  .rakuten-affiliate {
    max-width: 100%;
    overflow-x: hidden;
  }

  .rakuten-mobile {
    display: none;
  }

  @media (max-width: 640px) {
    .rakuten-desktop {
      display: none;
    }
    .rakuten-mobile {
      display: block;
      text-align: center;
    }
  }
</style>
